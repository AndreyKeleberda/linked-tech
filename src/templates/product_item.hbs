{{#each this}}
  <li id={{id}}>
    <h2>
      {{title}}
    </h2>
    <img src={{image}} alt={{title}} />
    <p>
      {{price}}
    </p>
    <p>
      {{category}}
    </p>
    <p>
      {{description}}
    </p>
  </li>
{{/each}}